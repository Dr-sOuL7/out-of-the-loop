# This bot is a long-polling WORKER, not a web service — it makes outbound
# calls to Telegram and listens on no port. Platforms that require a "web"
# process binding $PORT (e.g. Vercel/serverless) are NOT suitable; see DEPLOY.md.
worker: python run.py
